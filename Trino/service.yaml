apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: mynginx
  type: NodePort
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 80
      nodePort: 30080